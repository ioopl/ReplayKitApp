#  InAppClipsViewModel.swift & InAppClipsView.swift
 
## Record a rolling buffer of your In-App Screen Recording (A) and export the last 15 seconds as a video file. 

## Keep an efficient, in-memory circular queue of frame buffers to export the last 15-30 seconds of gameplay or app use without writing massive temporary files to disk

## Runs a rolling buffer to export and play the last 15 seconds of screen activity.

